# ==============================================
# AZURE PRODUCTION CONFIGURATION
# Copy this to deployment/prod.env and fill it in
# ==============================================

# --- General Config ---
# A unique name for your project (lowercase, alphanumeric, no spaces)
APP_NAME=pinnacle-hq
# Azure Region (e.g., eastus, westeurope, westus2)
LOCATION=eastus
# Resource Group Name
RESOURCE_GROUP=rg-pinnacle-prod

# --- Azure Container Registry (ACR) ---
# Must be globally unique, alphanumeric, no hyphens
ACR_NAME=pinnaclehqregistry

# --- Database Config ---
# Postgres Server Name (must be globally unique)
DB_SERVER_NAME=psql-pinnacle-prod
DB_ADMIN_USER=pinnacleadmin
DB_ADMIN_PASSWORD=ComplexPassword123!
DB_NAME=pinnacle_db

# --- Secrets & Auth ---
# Generate a secure random string (openssl rand -hex 32)
BETTER_AUTH_SECRET=
# Google OAuth Credentials (for Production)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
